# Plain Transformer baseline.
experiment:
  name: transformer_baseline
  seed: 42

data:
  window: 168
  horizon: 24
  batch_size: 64

model:
  name: transformer
  d_model: 128
  n_heads: 8
  num_layers: 3
  dropout: 0.1

training:
  epochs: 30
  lr: 0.001
  weight_decay: 0.0001
